# Copy to .env before running. Local SQLite DB path for Prisma (no secrets).
DATABASE_URL="file:./dev.db"

# Admin login for the Kanban board. Set your own, then run: npm run seed:admin
ADMIN_EMAIL="admin@studyplan.local"
ADMIN_PASSWORD="change-me"
